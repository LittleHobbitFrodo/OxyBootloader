[package]
name = "example_kernel"
description = "example kernel for Oxyboot"
version = "0.1.0"
edition = "2024"

[dependencies]
oxyboot_requests={ path="../oxyboot_requests", features = ["kernel"], version="1.0.0"}

[profile.release]
opt-level = 3
panic="abort"